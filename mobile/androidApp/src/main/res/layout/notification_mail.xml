<?xml version="1.0" encoding="utf-8"?>
<!--
The expanded mail notification (REQ-AND-PUSH-11, issue #348): sender,
subject, preview, and up to three attachment chips. Drawn as a custom big
content view under DecoratedCustomViewStyle, so the system still supplies
the header, the large icon and the actions.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="4dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/notification_sender"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textAppearance="@style/TextAppearance.Compat.Notification.Title" />

    <TextView
        android:id="@+id/notification_subject"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="2"
        android:ellipsize="end"
        android:textAppearance="@style/TextAppearance.Compat.Notification" />

    <TextView
        android:id="@+id/notification_preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="3"
        android:ellipsize="end"
        android:textAppearance="@style/TextAppearance.Compat.Notification" />

    <LinearLayout
        android:id="@+id/notification_chips"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="6dp"
        android:visibility="gone">

        <include layout="@layout/notification_chip_one" />

        <include layout="@layout/notification_chip_two" />

        <include layout="@layout/notification_chip_three" />
    </LinearLayout>
</LinearLayout>
