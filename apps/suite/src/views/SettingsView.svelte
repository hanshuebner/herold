<script lang="ts">
  // Settings UI scaffold. Real sections (theme, signature, push prefs, etc.)
  // land after the core features per docs/requirements/20-settings.md.
</script>

<div class="settings">
  <h1>Settings</h1>
  <p class="lead">Settings panel placeholder. Theme, default From, signature,
    push preferences, and the rest of REQ-SET-* arrive in phase 4.</p>
</div>

<style>
  .settings {
    padding: var(--spacing-07);
    max-width: 60ch;
  }
  h1 {
    font-size: var(--type-heading-03-size);
    line-height: var(--type-heading-03-line);
    font-weight: var(--type-heading-03-weight);
    margin: 0 0 var(--spacing-03);
  }
  .lead {
    font-size: var(--type-body-01-size);
    line-height: var(--type-body-01-line);
    color: var(--text-secondary);
    margin: 0;
  }
</style>
