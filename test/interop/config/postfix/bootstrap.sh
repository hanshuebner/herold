#!/bin/bash
# bootstrap.sh placeholder for postfix/docker-mailserver.
# The account file is pre-seeded via postfix-accounts.cf volume mount.
# No runtime bootstrap needed.
echo "postfix-bootstrap: accounts pre-seeded via postfix-accounts.cf"
