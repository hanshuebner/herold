// Package spam builds the spam-classification prompt and invokes the spam-classifier plugin. The plugin performs the actual LLM call; this package is pure logic plus one plugin invocation.
//
// Ownership: sieve-implementor.
package spam
