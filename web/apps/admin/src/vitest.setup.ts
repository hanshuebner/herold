/**
 * vitest setup file for the Admin SPA.
 *
 * Extends expect with @testing-library/jest-dom matchers so component tests
 * can use toBeInTheDocument(), toHaveAttribute(), toBeVisible(), etc.
 */
import '@testing-library/jest-dom/vitest';
