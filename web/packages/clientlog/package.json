{
  "name": "@herold/clientlog",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SPA client-log wrapper: captures errors, console output, and Web Vitals and batches them to herold's clientlog ingest endpoint.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "web-vitals": "^5.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.5",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.5"
  }
}
